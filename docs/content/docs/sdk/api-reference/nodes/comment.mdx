---
title: WeaveCommentNode
description: Comment node API Reference
---

import { TypeTable } from "fumadocs-ui/components/type-table";

## Overview

The [WeaveArrowNode](https://github.com/InditexTech/weavejs/blob/main/code/packages/sdk/src/nodes/arrow/arrow.ts)
class represents a basic arrow shape within the Weave.js canvas.

Built on top of Konva’s [Arrow](https://konvajs.org/api/Konva.Arrow.html) class shape under the hood, the WeaveArrowNode
offers a simple yet powerful primitive that supports full interaction, styling, and real-time synchronization.

Arrow are ideal for:

- Point to important stuff on whiteboards.
- Connect flows of ideas.

The class extends the [WeaveNode](/docs/sdk/api-reference/nodes/node) class

## Type

This node `nodeType` property value is `arrow`.

## Import

```shell
import { WeaveArrowNode } from "@inditextech/weave-sdk";
```

## Instantiation

```ts
new WeaveArrowNode(params?: WeaveArrowNodeParams);
```

## TypeScript types

```ts
type WeaveNodeTransformerProperties = Konva.TransformerConfig;

type WeaveArrowProperties = {
  transform: WeaveNodeTransformerProperties;
};

type WeaveArrowNodeParams = {
  config: Partial<WeaveArrowProperties>;
};
```

## Parameters

For `WeaveArrowNodeParams`:

<TypeTable
  type={{
    ["config"]: {
      required: true,
      description: "Config parameters for the node.",
      type: "Partial<WeaveArrowProperties>",
    },
  }}
/>

For `WeaveArrowProperties`:

<TypeTable
  type={{
    ["transform"]: {
      description:
        "Setup the transform properties for the arrow (if can be resized, rotated, anchors, etc.).",
      type: "object",
      default: "check default values",
    },
  }}
/>

## Default values

```ts
const WEAVE_TRANSFORMER_ANCHORS = {
  ["TOP_LEFT"]: "top-left",
  ["TOP_CENTER"]: "top-center",
  ["TOP_RIGHT"]: "top-right",
  ["MIDDLE_RIGHT"]: "middle-right",
  ["MIDDLE_LEFT"]: "middle-left",
  ["BOTTOM_LEFT"]: "bottom-left",
  ["BOTTOM_CENTER"]: "bottom-center",
  ["BOTTOM_RIGHT"]: "bottom-right",
};

const WEAVE_DEFAULT_ENABLED_ANCHORS: string[] = Object.values(
  WEAVE_TRANSFORMER_ANCHORS
);

const WEAVE_DEFAULT_TRANSFORM_PROPERTIES: WeaveNodeTransformerProperties = {
  rotateEnabled: true,
  resizeEnabled: true,
  enabledAnchors: WEAVE_DEFAULT_ENABLED_ANCHORS,
  borderStrokeWidth: 3,
  padding: 0,
};
```
