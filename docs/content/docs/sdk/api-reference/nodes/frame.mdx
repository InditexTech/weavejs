---
title: WeaveFrameNode
description: Frame node API Reference
---

import { TypeTable } from "fumadocs-ui/components/type-table";

## Overview

The [WeaveFrameNode](https://github.com/InditexTech/weavejs/blob/main/code/packages/sdk/src/nodes/frame/frame.ts)
class represents a specialized container node in Weave.js designed to act like a framed section or visual panel inside
the canvas. A WeaveFrameNode is used to group and organize nodes visually and logically within a bounded, styled
area—similar to a "frame" or "workspace" within the larger canvas.

Built on top of the [WeaveGroupNode](/docs/sdk/api-reference/nodes/group) and extended with additional visual and
interactive features, it provides a structured layout area perfect for modular designs, diagram sections, flowchart stages,
or collaborative workspaces.

Frames are useful for:

- Renders a rectangular frame with optional size, background color, border and title area (depending on design requirements).
- Acts as a container where users can add child nodes that logically belong inside a specific framed area.
- Allows dragging and interacting with the frame itself while maintaining the internal layout of its child nodes.
- Can carry additional metadata (e.g., frame labels, section types, or workflow states) useful for collaborative tooling and
  visual organization.

The class extends the [WeaveNode](/docs/sdk/api-reference/nodes/node) class

## Type

This node `nodeType` property value is `frame`.

## Import

```shell
import { WeaveFrameNode } from "@inditextech/weave-sdk";
```

## Instantiation

```ts
new WeaveFrameNode(params?: WeaveFrameNodeParams);
```

## TypeScript types

```ts
export const WEAVE_FRAME_NODE_SIZES_ORIENTATION = {
  landscape: "landscape",
  portrait: "portrait",
} as const;

export const WEAVE_FRAME_NODE_SIZES_TYPES = {
  A1: "A1",
  A2: "A2",
  A3: "A3",
  A4: "A4",
  CUSTOM: "custom",
} as const;

export const WEAVE_FRAME_NODE_SIZES: WeaveFrameNodeSizesInfo = {
  landscape: {
    A1: {
      width: 841,
      height: 594,
    },
    A2: {
      width: 592,
      height: 420,
    },
    A3: {
      width: 420,
      height: 297,
    },
    A4: {
      width: 297,
      height: 210,
    },
    custom: {
      width: 0,
      height: 0,
    },
  },
  portrait: {
    A1: {
      width: 594,
      height: 841,
    },
    A2: {
      width: 420,
      height: 594,
    },
    A3: {
      width: 297,
      height: 420,
    },
    A4: {
      width: 210,
      height: 297,
    },
    custom: {
      width: 0,
      height: 0,
    },
  },
};

type WeaveFrameNodeSizesOrientationKeys =
  keyof typeof WEAVE_FRAME_NODE_SIZES_ORIENTATION;
type WeaveFrameNodeSizesOrientation =
  (typeof WEAVE_FRAME_NODE_SIZES_ORIENTATION)[WeaveFrameNodeSizesOrientationKeys];

type WeaveFrameNodeSizesKeys = keyof typeof WEAVE_FRAME_NODE_SIZES_TYPES;
type WeaveFrameNodeSizes =
  (typeof WEAVE_FRAME_NODE_SIZES_TYPES)[WeaveFrameNodeSizesKeys];

type WeaveFrameNodeSizesInfo = {
  [Property in Partial<WeaveFrameNodeSizesOrientation>]: {
    [Property in WeaveFrameNodeSizes]: {
      width: number;
      height: number;
    };
  };
};

type WeaveNodeTransformerProperties = Konva.TransformerConfig;

type WeaveFrameProperties = {
  fontFamily: string;
  fontStyle: string;
  fontSize: number;
  fontColor: string;
  titleMargin: number;
  borderWidth: number;
  borderColor: string;
  onTargetLeave: {
    borderColor: string;
    fill: string;
  };
  onTargetEnter: {
    borderColor: string;
    fill: string;
  };
  transform: WeaveNodeTransformerProperties;
};

type WeaveFrameAttributes = WeaveElementAttributes & {
  title: string;
  frameWidth: number;
  frameHeight: number;
  frameOrientation: WeaveFrameNodeSizesOrientation;
  frameType: WeaveFrameNodeSizes;
};

type WeaveFrameNodeParams = {
  config: Partial<WeaveFrameProperties>;
};
```

## Parameters

For `WeaveFrameNodeParams`:

<TypeTable
  type={{
    ["config"]: {
      required: true,
      description: "Config parameters for the node.",
      type: "Partial<WeaveFrameProperties>",
    },
  }}
/>

For `WeaveFrameProperties`:

<TypeTable
  type={{
    ["fontFamily"]: {
      description: "The font family to use on the frame title.",
      type: "string",
      default: "check default values",
    },
    ["fontStyle"]: {
      description: "The font style to use on the frame title.",
      type: "string",
      default: "check default values",
    },
    ["fontSize"]: {
      description: "The font size to use on the frame title.",
      type: "string",
      default: "check default values",
    },
    ["fontColor"]: {
      description: "The font color to use on the frame title.",
      type: "string",
      default: "check default values",
    },
    ["titleMargin"]: {
      description: "The margin of the title (px) with the frame contents.",
      type: "number",
      default: "check default values",
    },
    ["borderWidth"]: {
      description: "The border width (px) of the frame.",
      type: "number",
      default: "check default values",
    },
    ["borderColor"]: {
      description: "The border color (HEX with alpha) of the frame.",
      type: "string",
      default: "#000check default values0ff",
    },
    ["onTargetLeave.borderColor"]: {
      description:
        "The border color (HEX with alpha) of the frame when no target is added.",
      type: "string",
      default: "check default values",
    },
    ["onTargetLeave.fill"]: {
      description:
        "The frame background color (HEX with alpha) of the frame when no target is added.",
      type: "string",
      default: "check default values",
    },
    ["onTargetEnter.borderColor"]: {
      description:
        "The border color (HEX with alpha) of the frame when a target is going to be added.",
      type: "string",
      default: "check default values",
    },
    ["onTargetEnter.fill"]: {
      description:
        "The frame background color (HEX with alpha) of the frame when a target going to be added.",
      type: "string",
      default: "check default values",
    },
    ["transform"]: {
      description:
        "Setup the transform properties for the frame (if can be resized, rotated, anchors, etc.).",
      type: "object",
      default: "check default values",
    },
  }}
/>

## Default values

```ts
const WEAVE_FRAME_NODE_DEFAULT_CONFIG = {
  fontFamily: "Arial",
  fontStyle: "normal",
  fontSize: 20,
  fontColor: "#000000ff",
  titleMargin: 20,
  borderColor: "#000000ff",
  borderWidth: 1,
  onTargetLeave: {
    borderColor: "#000000ff",
    fill: "#ffffffff",
  },
  onTargetEnter: {
    borderColor: "#ff6863ff",
    fill: "#ffffffff",
  },
  transform: {
    rotateEnabled: false,
    resizeEnabled: false,
    enabledAnchors: [] as string[],
    borderStrokeWidth: 3,
    padding: 0,
  },
};

const WEAVE_FRAME_NODE_DEFAULT_PROPS = {
  title: "Frame XXX",
  frameWidth: (WEAVE_FRAME_NODE_SIZES.landscape.A4.width *
    WEAVE_FRAME_NODE_SIZES_MULTIPLIER) as number,
  frameHeight: (WEAVE_FRAME_NODE_SIZES.landscape.A4.height *
    WEAVE_FRAME_NODE_SIZES_MULTIPLIER) as number,
  frameType: WEAVE_FRAME_NODE_SIZES_TYPES.A4 as string,
  frameOrientation: WEAVE_FRAME_NODE_SIZES_ORIENTATION.landscape as string,
};
```
