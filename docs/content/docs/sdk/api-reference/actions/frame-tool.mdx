---
title: WeaveFrameToolAction
description: Allows to add a canvas in canvas area
---

import { TypeTable } from "fumadocs-ui/components/type-table";

## Overview

The [WeaveFrameToolAction](https://github.com/InditexTech/weavejs/blob/main/code/packages/sdk/src/actions/frame-tool/frame-tool.ts)
class that allows users to create a new Frame node by clicking and dragging on the canvas. A frame acts
as a visual container for organizing other nodes and structuring the workspace—similar to sections, groups,
or modules in visual editors.

This action enables an intuitive, drag-to-create experience for defining new framed areas, which are immediately
editable and collaborative.

Each interaction results in the creation of a [WeaveFrameNode](/docs/sdk/api-reference/nodes/frame),
which can be moved and synchronized across all connected users.

The class extends the [WeaveAction](/docs/sdk/api-reference/actions/action) class.

## Name

This action `name` property value is `frameTool`.

## Import

```shell
import { WeaveFrameToolAction } from "@inditextech/weave-sdk";
```

## Instantiation

```ts
new WeaveFrameToolAction();
```

## TypeScript types

```ts
type WeaveFrameToolActionTriggerParams = {
  title?: string;
  frameWidth?: number;
  frameHeight?: number;
  frameOrientation?: string;
  frameType?: string;
};
```

## Trigger function params

```ts
trigger(cancelAction: () => void, params?: WeaveFrameToolActionTriggerParams): void;
```

For `WeaveFrameToolActionTriggerParams`:

<TypeTable
  type={{
    ["title"]: {
      description: "The title of the Frame.",
      type: "string",
      default: "Frame XXX",
    },
    ["frameWidth"]: {
      description: "The frame format width (px) for the Frame.",
      type: "number",
      default: "1485",
    },
    ["frameHeight"]: {
      description: "The frame format height (px) for the Frame.",
      type: "number",
      default: "1050",
    },
    ["frameOrientation"]: {
      description:
        "The frame format orientation (landscape or portrait) for the Frame.",
      type: "string",
      default: "landscape",
    },
    ["frameType"]: {
      description:
        "The frame format type (A1, A2, A3, A4 or custom) for the Frame.",
      type: "string",
      default: "A4",
    },
  }}
/>
