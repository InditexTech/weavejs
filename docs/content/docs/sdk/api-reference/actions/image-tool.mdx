---
title: WeaveImageToolAction
description: Add an image to the canvas
---

import { TypeTable } from "fumadocs-ui/components/type-table";

## Overview

The [WeaveImageToolAction](https://github.com/InditexTech/weavejs/blob/main/code/packages/sdk/src/actions/image-tool/image-tool.ts)
class that enables users to insert image nodes into the canvas. It provides
an intuitive way to add visual content—either by selecting an image file, dragging
and dropping, or integrating with an image picker—allowing users to enhance their
collaborative workspace with rich media.

Each interaction results in the creation of a [WeaveImageNode](/docs/sdk/api-reference/nodes/image),
which can be resized, moved, and synchronized across all connected users.

The class extends the [WeaveAction](/docs/sdk/api-reference/actions/action) class.

## Name

This action `name` property value is `imageTool`.

## Import

```shell
import { WeaveImageToolAction } from "@inditextech/weave-sdk";
```

## Instantiation

```ts
new WeaveImageToolAction();
```

## TypeScript types

```ts
type WeaveImageToolActionLoadFromKeys = keyof typeof IMAGE_TOOL_LOAD_FROM;
type WeaveImageToolActionLoadFrom =
  (typeof IMAGE_TOOL_LOAD_FROM)[WeaveImageToolActionLoadFromKeys];

type WeaveImageToolActionStateKeys = keyof typeof IMAGE_TOOL_STATE;
type WeaveImageToolActionState =
  (typeof IMAGE_TOOL_STATE)[WeaveImageToolActionStateKeys];

type WeaveImageToolActionOnStartLoadImageEvent = undefined;
type WeaveImageToolActionOnEndLoadImageEvent = Error | undefined;
type WeaveImageToolActionOnAddingEvent = { imageURL: string };
type WeaveImageToolActionOnAddedEvent = {
  imageURL: string;
  nodeId: string;
};

type WeaveImageToolActionTriggerCommonParams = {
  imageId?: string;
  options?: ImageOptions;
  position?: Konva.Vector2d;
  forceMainContainer?: boolean;
};

type WeaveImageToolActionTriggerParams = (
  | {
      type: typeof WEAVE_IMAGE_TOOL_UPLOAD_TYPE.FILE;
      imageFile: File;
      imageDownscaleRatio: number;
    }
  | {
      type: typeof WEAVE_IMAGE_TOOL_UPLOAD_TYPE.IMAGE_URL;
      imageURL: string;
      imageFallback: string;
      imageWidth: number;
      imageHeight: number;
    }
) &
  WeaveImageToolActionTriggerCommonParams;

type ImageOptions = {
  crossOrigin: ImageCrossOrigin;
};

type WeaveImageToolActionTriggerReturn =
  | {
      nodeId: string;
      finishUploadCallback: (nodeId: string, imageURL: string) => void;
    }
  | undefined;

type WeaveImageToolDragAndDropProperties = {
  imageURL: string;
  imageFallback: string;
  imageWidth: number;
  imageHeight: number;
  imageId?: string;
};

type WeaveImageToolActionConfig = {
  style: {
    cursor: {
      padding: number;
      imageThumbnail: {
        width: number;
        height: number;
        shadowColor: string;
        shadowBlur: number;
        shadowOffset: Konva.Vector2d;
        shadowOpacity: number;
      };
    };
  };
};

type WeaveImageToolActionParams = {
  config: DeepPartial<WeaveImageToolActionConfig>;
};
```

## Trigger function params

```ts
trigger(cancelAction: () => void, params?: WeaveImageToolActionTriggerParams): WeaveImageToolActionTriggerReturn;
```

For `WeaveImageToolActionTriggerParams`:

<TypeTable
  type={{
    ["type"]: {
      description: "The type of the upload image, can be 'file' or 'imageURL'",
      type: "string",
    },
    ["imageFile"]: {
      description:
        "The File of the image. Must be provided when adding images from a file-picker, on external paste images or when drag-and-drop images from the file-system",
      type: "File",
    },
    ["imageDownscaleRatio"]: {
      description: "The downscale ratio to apply to the uploaded file",
      type: "number",
    },
    ["imageURL"]: {
      description:
        "The URL of the image already uploaded to an external service. URL must the same as the application URL to avoid cross-origin issues. We recommend to proxy the images through the application. Must be provided when drag & drop from the same application.",
      type: "string",
    },
    ["imageFallback"]: {
      description:
        "A dataURL of a fallback image to display while the real image is loaded asynchronously.",
      type: "string",
    },
    ["imageWidth"]: {
      description:
        "The width of the image without transformations, must be provided when drag and drop images from the same application (as this normally already have been persisted).",
      type: "string",
    },
    ["imageHeight"]: {
      description:
        "The height of the image without transformations, must be provided when drag and drop images from the same application (as this normally already have been persisted).",
      type: "string",
    },
    ["imageId"]: {
      description:
        "An external Id for the image. It's saved as imageId prop on the node.",
      type: "string",
    },
    ["options"]: {
      description:
        "The options for the HTMLImageElement DOM element. Useful for CORS support.",
      type: "any",
    },
    ["position"]: {
      description:
        "Position the image in this coordinates provided by the vector.",
      type: "Vector2d",
    },
    ["forceMainContainer"]: {
      description:
        "Force the image to be added in the main container and not on the container identified by the pointer.",
      type: "boolean",
    },
  }}
/>

When triggering the tool, a `WeaveImageToolActionTriggerReturn` object is returned, this object contains:

- `nodeId`: is the Id of the node added to the canvas.
- `finishUploadCallback`: a callback function to call to save on the node the persisted Image URL. This function receives the `nodeId` and the persisted image URL.

### Drag & Drop from the same application

When you implement drag & drop of an image loaded on a HTMLImageElement that is located on the same application,
on the `onDragStart` event you must call the `setDragAndDropProperties` method of the tool and define the following
properties:

- `imageURL`: the image URL to add to the canvas.
- `imageFallback`: the fallback image (as dataURL) to set while the final image is asynchronously loaded.
- `imageId`: the image id (external resource id) associated to the image.
- `imageWidth`: the image real width (without any transformations).
- `imageHeight`: the image real height (without any transformations).

## Events

### onImageLoadStart

```ts
onImageLoadStart: WeaveNodesSelectionPluginOnNodesChangeEvent;
```

The `onImageLoadStart` event is called when the image is stated to load onto the canvas.

### onImageLoadEnd

```ts
onImageLoadEnd: WeaveNodesSelectionPluginOnNodesChangeEvent;
```

The `onImageLoadEnd` event is called when the image ended loading into the canvas.

## Methods

### setDragAndDropProperties

```ts
setDragAndDropProperties(properties: WeaveImageToolDragAndDropProperties): void
```

The `setDragAndDropProperties` method set the necessary properties when drag & drop images that are inside the application.
