---
title: WeaveStageGridPlugin
description: Renders a reference grid behind the nodes layer
---

import { TypeTable } from "fumadocs-ui/components/type-table";

## Overview

The [WeaveStageGridPlugin](https://github.com/InditexTech/weavejs/blob/main/code/packages/sdk/src/plugins/stage-grid/stage-grid.ts)
class that renders a background grid on the canvas Stage. It provides visual guides that help users align, arrange,
and position nodes more precisely, improving layout consistency and the overall user experience.

The plugin can render grids as lines or dots, and it is highly configurable to match the needs of different visual
collaborative tools, from whiteboards to complex design editors.

The class extends the [WeavePlugin](/docs/sdk/api-reference/plugins/plugin) class

## Name

This plugin `name` property value is `stageGrid`.

## Import

```shell
import { WeaveStageGridPlugin } from "@inditextech/weave-sdk";
```

## Instantiation

```ts
new WeaveStageGridPlugin(params?: Partial<WeaveStageGridPluginParams>);
```

## TypeScript types

```ts
type WeaveStageGridPluginConfig = {
  type: WeaveStageGridType;
  gridColor: string;
  gridOriginColor: string;
  gridSize: number;
  gridMajorRatio?: number;
  gridStroke?: number;
  gridDotRadius?: number;
};

type WeaveStageGridPluginParams = {
  config?: Partial<WeaveStageGridPluginConfig>;
};
```

## Parameters

For `WeaveStageGridPluginParams`:

<TypeTable
  type={{
    ["config"]: {
      description: "Config parameters for the plugin.",
      type: "Partial<WeaveStageGridPluginConfig>",
    },
  }}
/>

For `WeaveStageGridPluginConfig`:

<TypeTable
  type={{
    ["type"]: {
      description: "Type of the grid to use by default.",
      type: "WeaveStageGridType",
      default: "lines",
    },
    ["gridColor"]: {
      description: "Color of the grid (lines or dots)",
      type: "string",
      default: "rgba(0,0,0,0.2)",
    },
    ["gridOriginColor"]: {
      description: "Color of the grid origin axis.",
      type: "string",
      default: "rgba(255,0,0,0.2)",
    },
    ["gridSize"]: {
      description:
        "A number in pixels that define the size of the grid separation.",
      type: "number",
      default: "50",
    },
    ["gridMajorEvery"]: {
      description:
        "A number that defines when to render the major lines, every N gridSize",
      type: "number",
      default: "10",
    },
    ["gridMajorRatio"]: {
      description:
        "A number that defines the size (ratio) of the references, for the type lines its a multiple for the stroke and for the dots its a multiple for the dot radius",
      type: "number",
      default: "type === 'lines' => 4, type === 'dots' => 2",
    },
    ["gridStroke"]: {
      description:
        "A number that defines the stroke base size of the grid lines when the type is lines",
      type: "number",
      default: "0.5",
    },
    ["gridDotRadius"]: {
      description:
        "A number that defines the dot radius base size of the grid when the type is dots",
      type: "number",
      default: "1",
    },
  }}
/>

## Methods

### getType

```ts
getType(): WeaveStageGridType
```

This method return the type of the actual grid (line or dotted).

### setType

```ts
setType(type: WeaveStageGridType): void
```

This method set the type of the actual grid (line or dotted).

### enable

```ts
enable(): void
```

This method enables the grid.

### disable

```ts
disable(): void
```

This method disables the grid.
