---
title: Stroke
description: Draw a free-line on the canvas
---

![Stroke on Weave.js showcase](/images/nodes/stroke.png)

The Stroke node represents a customizable free-line on the canvas.
It supports features like stroke color, width, dash patterns.

## Usage

<div className="fd-steps">

<div className="[&_h3]:fd-step">

### Import the Node

Start by importing the node:

```ts
import { WeaveStrokeNode } from "@inditextech/weave-sdk";
```

</div>

<div className='[&_h3]:fd-step'>

### Register the Node

Then register the node on the [Weave](/docs/sdk/api-reference/weave) class instance.

```ts
const instance = new Weave({
  ...
  nodes: [
    ...,
    new WeaveStrokeNode(), // [!code ++]
  ]
})
```

</div>

<div className='[&_h3]:fd-step'>

### Use the node

Once the node is registered you can use it on [Actions](/docs/main/build/actions) or
[Plugins](/docs/main/build/plugins) or even other [Nodes](/docs/main/build/nodes).

We provide a single action:

- [Brush Tool](/docs/main/build/actions/brush-tool) that allows users to add free-line nodes.

</div>

</div>
