---
title: WeaveStarNode
description: Ellipse node API Reference
---

import { TypeTable } from "fumadocs-ui/components/type-table";

## Overview

The [WeaveStarNode](https://github.com/InditexTech/weavejs/blob/main/code/packages/sdk/src/nodes/star/star.ts)
class represents a basic star shape within the Weave.js canvas.

Built on top of Konva’s [Star](https://konvajs.org/api/Konva.Star.html) class shape under the hood, the WeaveStarNode
offers a simple yet powerful primitive that supports full interaction, styling, and real-time synchronization.

The class extends the [WeaveNode](/docs/sdk/api-reference/nodes/node) class

## Type

This node `nodeType` property value is `star`.

## Import

```shell
import { WeaveStarNode } from "@inditextech/weave-sdk";
```

## Instantiation

```ts
new WeaveStarNode(params?: WeaveStarNodeParams);
```

## TypeScript types

```ts
type WeaveNodeTransformerProperties = Konva.TransformerConfig;

type WeaveStarProperties = {
  transform: WeaveNodeTransformerProperties;
};

type WeaveStarNodeParams = {
  config: Partial<WeaveStarProperties>;
};
```

## Parameters

For `WeaveStarNodeParams`:

<TypeTable
  type={{
    ["config"]: {
      required: true,
      description: "Config parameters for the node.",
      type: "Partial<WeaveStarProperties>",
    },
  }}
/>

For `WeaveStarProperties`:

<TypeTable
  type={{
    ["transform"]: {
      description:
        "Setup the transform properties for the star (if can be resized, rotated, anchors, etc.).",
      type: "object",
      default: "check default values",
    },
  }}
/>

## Default values

```ts
const WEAVE_TRANSFORMER_ANCHORS = {
  ["TOP_LEFT"]: "top-left",
  ["TOP_CENTER"]: "top-center",
  ["TOP_RIGHT"]: "top-right",
  ["MIDDLE_RIGHT"]: "middle-right",
  ["MIDDLE_LEFT"]: "middle-left",
  ["BOTTOM_LEFT"]: "bottom-left",
  ["BOTTOM_CENTER"]: "bottom-center",
  ["BOTTOM_RIGHT"]: "bottom-right",
};

const WEAVE_DEFAULT_ENABLED_ANCHORS: string[] = Object.values(
  WEAVE_TRANSFORMER_ANCHORS
);

const WEAVE_DEFAULT_TRANSFORM_PROPERTIES: WeaveNodeTransformerProperties = {
  rotateEnabled: true,
  resizeEnabled: true,
  enabledAnchors: WEAVE_DEFAULT_ENABLED_ANCHORS,
  borderStrokeWidth: 3,
  padding: 0,
};
```
